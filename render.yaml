services:
  - type: web
    name: smartsip
    env: python
    buildCommand: "pip install -r requirements.txt && python generate_data.py && python train_models.py"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
